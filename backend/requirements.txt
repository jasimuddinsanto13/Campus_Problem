# --- Web frameworks ---
Django>=5.2,<5.3  # 5.2 LTS: supports MySQL 8.0 and Python 3.14 (6.x requires MySQL 8.4+)
fastapi>=0.141
uvicorn[standard]>=0.52
python-multipart>=0.0.6

# --- Database ---
PyMySQL>=1.1
SQLAlchemy>=2.0

# --- Config ---
python-dotenv>=1.0

# --- Utilities ---
python-docx>=1.1
Pillow>=10.0  # required by Django ImageField (profile pictures)

# --- AI chat assistant (Google Gemini, Interactions API) ---
# Official current SDK (google-generativeai is deprecated). Requires
# GEMINI_API_KEY in .env; without it the chat widget simply returns 503.
google-genai>=2.3.0

# --- Push notifications (Firebase Cloud Messaging) ---
# Sends "Class Cancelled" pushes to matched students' devices. Requires
# FCM_SERVICE_ACCOUNT_PATH in backend/.env pointing at a Firebase service
# account JSON; without it the app simply runs without OS-level push.
firebase-admin>=6.0
