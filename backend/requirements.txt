# ---------------------------------------------------------------------------
# Campus Problem — Django backend requirements
# PythonAnywhere production deployment (SQLite3)
# ---------------------------------------------------------------------------

# Core Django
Django==5.2.17
gunicorn==26.1.0
python-dotenv==1.2.2

# Image processing (profile pictures, etc.)
pillow==12.3.0

# Document generation (routine PDF/DOCX downloads)
python-docx==1.2.0
lxml==6.1.1

# HTTP client (for external API calls, webhooks)
requests==2.34.2

# JWT tokens (authentication)
PyJWT==2.13.0

# HTTP caching
CacheControl==0.14.4

# SQL parsing (Django dependency)
sqlparse==0.5.5

# Firebase Cloud Messaging (optional — push notifications)
# Install only if you need FCM push. Without it the app works fine,
# just minus OS-level push notifications.
firebase_admin==7.5.0
