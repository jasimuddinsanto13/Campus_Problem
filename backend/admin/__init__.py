"""Admin portal app — the admin section of the campus platform.

Holds every piece of code that belongs to the Admin role:
  * the admin dashboard (pending registrations + account stats)
  * the approve / reject actions for student & faculty registrations
  * the admin dashboard template

Mounted at /portal/admin/ (namespace `admin_portal`).
"""
